package leetcode;

public class StrStr {
    int strStr(String haystack, String needle) {
        if (needle == "") return 0;
        int index = haystack.indexOf(needle);

        return index;
    }
}
